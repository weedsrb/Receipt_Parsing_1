currency: "$"
date_format: "%d/%m/%Y"

types:
  float:
    - paid amount
    - cash
    - total
    - price

  int:
    - quantity
    - items count
    - order no.

dont_include:
  - total

date_time:
  - pattern: '(\d{1,2}[/.-]\d{1,2}[-./]\d{4})'
    field: "Date"

  - pattern: '(\d{2}:\d{2}:\d{2}\s*(?:[APap][Mm])?)'
    field: "Time"

items:
  separator: "x"
  pattern: '(\d+)\s+{separator}\s+{currency}(\d+\.\d{{2}})'